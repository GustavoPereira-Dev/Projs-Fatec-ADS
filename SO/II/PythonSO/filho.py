#/usr/bin/python3
#/tmp/filho.py

import json, sys;

data sys.stdin.readlines();
input_process = json.loads(data[0].rstrip());

print("Inside the child, the parent process sent: input_process['message'));